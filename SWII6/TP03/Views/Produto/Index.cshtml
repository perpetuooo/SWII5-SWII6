@model IEnumerable<TP03.Models.Produto>

<div class="container mt-5 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">Lista de Produtos</h2>
                </div>
                <div class="card-body">
                    @if (!Model.Any())
                    {
                        <div class="alert alert-warning" role="alert">
                            Nenhum produto encontrado.
                        </div>
                    }
                    else
                    {
                        <table class="table table-striped table-bordered table-hover">
                            <thead class="thead-light">
                                <tr>
                                    <th>Nome</th>
                                    <th>Preço</th>
                                    <th>Quantidade</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var produto in Model)
                                {
                                    <tr>
                                        <td>@produto.Nome</td>
                                        <td>@produto.Preco.ToString("C2")</td>
                                        <td>@produto.Quantidade</td>
                                        <td>
                                            <a href="@Url.Action("Details", "Produto", new { id = produto.ID })" class="btn btn-info btn-sm text-white">
                                                Detalhes
                                            </a>
                                            <form method="post" asp-action="Delete" asp-route-id="@produto.ID" style="display:inline;">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir este produto?')">
                                                    <i class="fas fa-trash-alt"></i> Excluir
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Pedro H Perpétuo CB3021688 & Igor Benites CB3021734 -->